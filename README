== my things

Track your things with a simple Rails Application.

== Demo

Demo: http://things.fabiokuhn.com

== Setup

Edit the file "db/seeds.rb" and add a "config/database.yml".

Run "rake gems:install" to install all required gems.
Run "rake db:create db:migrate db:seed" to setup the database.

== Usage

You can start the server with the command "script/server" or whatever command you use to start your server.
Visit the site "/login" with your browser and enter your password. If it doesn't work recheck your "db/seeds.rb" file and run rake db:seed again.
